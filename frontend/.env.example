# 本地后端开发：前端直接请求本机 FastAPI
VITE_API_BASE_URL=http://127.0.0.1:8000

# Vercel 同源部署：在 Vercel 环境变量中将 VITE_API_BASE_URL 留空或不设置。
# 当前前端服务层请求路径已经是 /api/v1/...，留空后会请求同源 API，
# 由项目根目录 vercel.json 将 /api/* 转发到 FastAPI Serverless 入口。
